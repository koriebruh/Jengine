# proto/jengine/v1

Empty on purpose. Populated starting `plans/task/core/10` (matching engine event schema) and `plans/task/core/15` (API layer, `.proto`-first via Connect-RPC). See `plans/docs/06-streaming-architecture.md` §7.2 and `plans/docs/07-api-extensibility.md` §8.1.

Do not author `.proto` files here before those tasks — this file exists only so the path is present in the repo skeleton from task 01 onward.
